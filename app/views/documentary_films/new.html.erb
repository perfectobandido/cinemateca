<h1>Nuevo Documental</h1>

<%= form_with(model: @documentary_film, url: documentary_films_path, local: true) do |form| %>
  <%= render 'shared/errors', object: @documentary_film  %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :synopsis %>
    <%= form.text_area :synopsis %>
  </div>

  <div>
    <%= form.label :director %>
    <%= form.text_field :director %>
  </div>

  <div>
    <%= form.submit "Crear Documental", class: 'btn btn-warning'  %>
    <%= link_to 'Volver atrás', documentary_films_path, class: 'btn btn-primary' %>
  </div>
<% end %>


<div class="d-flex justify-content-between align-items-center">
  <%= link_to 'Nueva Serie', "/series/new", class: 'btn btn-primary' %>
  
</div>
<h1>Lista de Series</h1>
<ul>
  <% @series.each do |serie| %>
    <li>
      <strong>Name:</strong> <%= serie.name %><br>
      <strong>Synopsis:</strong> <%= serie.synopsis %><br>
      <strong>Director:</strong> <%= serie.director %>
    </li>
  <% end %>
</ul>
